@model Topee.Models.shop_car
@{
    ViewBag.Title = "goods_Delete";
}

<h2>刪除品項</h2>
<h3>Are you sure you want to delete this?</h3>
<div>
    <tr>
        <td>
            <img class="img-rounded img-responsive" src="@Model.Goods.url" width="150" />
        </td>

        <td>
        </td>
            <div>@Html.DisplayFor(modelItem => Model.Goods.name)</div>
        <td>
            <div style="color:red">$@Html.DisplayFor(modelItem => Model.Goods.price)</div>
        </td>
    </tr>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="確認" class="btn btn-default" />
            @Html.ActionLink("返回購物車", "Shop_car")
        </div>
    }
</div>
